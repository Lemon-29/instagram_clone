<div class="users_h1_box"><h1>Check the Details</h1></div>
<p id="notice"><%= notice %></p>
<p>
 <% unless @picture.user_id == current_user.id %>
  <% if @favorite.present? %>
   <%= link_to "Remove form favorite", favorite_path(id: @favorite.id), method: :delete, class: "btn btn-danger" %>
  <% else %>
   <%= link_to "Add as favorite", favorites_path(picture_id: @picture.id), method: :post, class: "btn btn-primary" %>
  <% else %>
</p>

<p>
 <strong>Poster:</strong><%= @picture.user.name %>
</p>

<p>
 <strong>Title:</strong><%= @picture.title %>
</p>

<p>
 <strong>Text:</strong><%= @picture.content %>¥
</p>

<% if @picture.image.url %>
 <p>
  <strong>Image</strong>
  <%= image_tag @picture.image.url %>
</p>
<% end %>

<%= link_to "Back", picture.path %>

   